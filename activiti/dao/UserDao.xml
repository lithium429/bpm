<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xz.project.activiti.service.UserService">  
	<select id="identi" parameterType="map" resultType="com.xz.project.activiti.pojo.User">
	    select * from act_id_user where id=#{id} and password=#{password}
	</select>
</mapper>